{
	"name": "coas-daemon",
	"version": "1.2.0",
	"description": "CoAS daemon: scheduler tick, agent registry, and A2A routing (ADR-0018).",
	"type": "module",
	"main": "dist/main.js",
	"private": true,
	"engines": {
		"node": ">=22"
	},
	"scripts": {
		"build": "tsc -p tsconfig.emit.json"
	}
}
