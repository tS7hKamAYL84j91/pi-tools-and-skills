# Teams Graph Affordances Compliance (Superseded)

**Status:** Superseded on 2026-05-04 by `docs/teams-simplification.md`.

This compliance record evaluated an earlier plan to expand a generic graph executor. That plan is no longer active. The current standing decision is direct protocol handlers behind the `TeamHandler` boundary, with no generic DAG executor.
