# P2 Hygiene Plan

## Target shape

1. Replace user/tool-derived regex construction with escaped matching and replace chained prompt escaping with a single-pass encoder.
2. Make `extensions/pi-coas/lib/coas-run-state.ts` the sole implementation of run-state persistence. Keep scheduler-layer history/summary APIs and re-export the shared run-state type/functions from `scheduler-run-state.ts`.
3. Triage each flagged lens site: add explicit safe types/guards where behavior is unchanged; document intentional fail-closed throws and justified compatibility casts.

## Constraints and acceptance criteria

- Preserve all existing public exports and fail-closed behavior.
- Touch only requested source paths plus this plan and the architecture model if required by repository policy.
- `npm run check` and `npm test` pass, with the test-file count recorded.
- Review the final diff for excluded paths and list every flagged site disposition.

## Review plan

- Inspect surrounding behavior and existing tests before edits.
- Run targeted tests after edits, then the full check and test suites.
- Verify no unescaped dynamic regex or chained `replaceAll` remains at the three security sites and no scheduler implementation duplication remains.
