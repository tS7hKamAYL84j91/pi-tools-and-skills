# Search scripts for deep-research skill
# All scripts use stdlib urllib by default — these are optional enhancements.

# Optional: cleaner HTTP + retry logic (stdlib urllib used as fallback)
requests>=2.28.0

# Optional: Tavily search engine (primary web search, requires TAVILY_API_KEY)
# pip install tavily-python>=0.5.0

# No dependencies needed for:
#   - Semantic Scholar (scholarly_api.py) — uses stdlib, optional S2_API_KEY
#   - GitHub search (github_search.py) — uses stdlib, optional GITHUB_TOKEN
#   - DuckDuckGo search (web_scraper.py --engine duckduckgo) — uses stdlib, no key
#   - Jina URL reader (web_scraper.py --url) — uses stdlib, no key