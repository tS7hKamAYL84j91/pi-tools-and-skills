---
tool: {tool-name}
version: ">={minimum-version}"
updated: YYYY-MM-DD
category: {category}        # e.g. version-control, containers, networking, language
tags: [{tag1}, {tag2}, {tag3}]
confidence: high            # high | medium | low  (reflects provenance quality)
---

# {tool} — {one-line purpose}

> {What it does in ≤20 words.}

## Common operations

- {Intent / task description}:
  `{command with {{placeholders}}}`

- {Intent / task description}:
  `{command with {{placeholders}}}`

- {Intent / task description}:
  `{command with {{placeholder}} and {{another-placeholder}}}`

## Patterns

- {Recurring multi-step workflow or compound command}:
  `{step-1-command} && {step-2-command}`

- {Common pipeline or flag combination}:
  `{command} | {filter}`

## Gotchas

- {Common mistake or failure mode — and how to avoid it}
- {Dangerous default behaviour that surprises users}
- {When NOT to use this command / flag}

## Examples

- {End-to-end scenario: what you want and the exact invocation}:
  `{command}`

- {Multi-step scenario with context}:
  ```
  {line 1}
  {line 2}
  ```
